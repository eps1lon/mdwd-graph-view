(function ($window) {
    // export patternß
    window.domainSelector = (function () {
        return {
            className: 'DomainSelector'
            // class methods
        }
    })()
})(this)